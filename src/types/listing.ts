export type Listing = {
  id: string,
  title: string,
  description: string,
  imageSrc: string,
  createdAt: string,
  category: string,
  roomCount: number,
  bathroomCount: number,
  guestCount: number,
  locationValue: string,
  userId: string,
  price: number
}
